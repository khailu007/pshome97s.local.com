jQuery(document).ready(function ($) {
	// Validate tel on contact form 7 (Review/vi)
	$('#contact_form_phone_21059').keypress(function(e) {
		var ev = e || window.event;
		var key = ev.keyCode || ev.which;
		key = String.fromCharCode( key );

		// Validate first character
		var text = $('#contact_form_phone_21059').val();
		var y = text.length;
		if((y + 1) == 1) {
			var regexFirst = /[0]/;
			if( !regexFirst.test(key) ) {
				ev.returnValue = false;
				if(ev.preventDefault) ev.preventDefault();
			}
		}

		// Validate only input number
		var regex = /[0-9]/;		
		if( !regex.test(key) ) {
			ev.returnValue = false;
			if(ev.preventDefault) ev.preventDefault();
		}
		// Validate length 10
		if((y + 1) > 10) {
			ev.returnValue = false;
			if(ev.preventDefault) ev.preventDefault();
		}		
	});

	// Validate tel on contact form 7 (Review/en)
	$('#contact_form_phone_21061').keypress(function(e) {
		var ev = e || window.event;
		var key = ev.keyCode || ev.which;
		key = String.fromCharCode( key );

		// Validate first character
		var text = $('#contact_form_phone_21061').val();
		var y = text.length;
		if((y + 1) == 1) {
			var regexFirst = /[0]/;
			if( !regexFirst.test(key) ) {
				ev.returnValue = false;
				if(ev.preventDefault) ev.preventDefault();
			}
		}

		// Validate only input number
		var regex = /[0-9]/;		
		if( !regex.test(key) ) {
			ev.returnValue = false;
			if(ev.preventDefault) ev.preventDefault();
		}
		// Validate length 10
		if((y + 1) > 10) {
			ev.returnValue = false;
			if(ev.preventDefault) ev.preventDefault();
		}		
	});	

	// Validate tel on contact form 7 (Register/vi)
	$('#contact_form_phone_20984').keypress(function(e) {
		var ev = e || window.event;
		var key = ev.keyCode || ev.which;
		key = String.fromCharCode( key );

		// Validate first character
		var text = $('#contact_form_phone_20984').val();
		var y = text.length;
		if((y + 1) == 1) {
			var regexFirst = /[0]/;
			if( !regexFirst.test(key) ) {
				ev.returnValue = false;
				if(ev.preventDefault) ev.preventDefault();
			}
		}

		// Validate only input number
		var regex = /[0-9]/;		
		if( !regex.test(key) ) {
			ev.returnValue = false;
			if(ev.preventDefault) ev.preventDefault();
		}
		// Validate length 10
		if((y + 1) > 10) {
			ev.returnValue = false;
			if(ev.preventDefault) ev.preventDefault();
		}		
	});

	// Validate tel on contact form 7 (Register/en)
	$('#contact_form_phone_20986').keypress(function(e) {
		var ev = e || window.event;
		var key = ev.keyCode || ev.which;
		key = String.fromCharCode( key );

		// Validate first character (Register/en)
		var text = $('#contact_form_phone_20986').val();
		var y = text.length;
		if((y + 1) == 1) {
			var regexFirst = /[0]/;
			if( !regexFirst.test(key) ) {
				ev.returnValue = false;
				if(ev.preventDefault) ev.preventDefault();
			}
		}

		// Validate only input number
		var regex = /[0-9]/;		
		if( !regex.test(key) ) {
			ev.returnValue = false;
			if(ev.preventDefault) ev.preventDefault();
		}
		// Validate length 10
		if((y + 1) > 10) {
			ev.returnValue = false;
			if(ev.preventDefault) ev.preventDefault();
		}		
	});		

	// Validate email on contact form 7 (Review/vi)
	$('#contact_form_email_21059').keyup(function(e) {
		var email = $('#contact_form_email_21059').val();
		// Validate only input number
		var regex = /^([\w-\.]+)@((\[[0-9]{1,3}\.[0-9]{1,3}\.[0-9]{1,3}\.)|(([\w-]+\.)+))([a-zA-Z]{2,4}|[0-9]{1,3})(\]?)$/;	
		if( !regex.test(email) ) {
			$('.wpcf7-submit').hide();
		}
		else
		{
			$('.wpcf7-submit').show();
		}	
	});	

	// Validate email on contact form 7 (Review/en)
	$('#contact_form_email_21061').keyup(function(e) {
		var email = $('#contact_form_email_21061').val();
		// Validate only input number
		var regex = /^([\w-\.]+)@((\[[0-9]{1,3}\.[0-9]{1,3}\.[0-9]{1,3}\.)|(([\w-]+\.)+))([a-zA-Z]{2,4}|[0-9]{1,3})(\]?)$/;	
		if( !regex.test(email) ) {
			$('.wpcf7-submit').hide();
		}
		else
		{
			$('.wpcf7-submit').show();
		}	
	});		

	// Validate email on contact form 7 (Register/vi)
	$('#contact_form_email_20984').keyup(function(e) {
		var email = $('#contact_form_email_20984').val();
		// Validate only input number
		var regex = /^([\w-\.]+)@((\[[0-9]{1,3}\.[0-9]{1,3}\.[0-9]{1,3}\.)|(([\w-]+\.)+))([a-zA-Z]{2,4}|[0-9]{1,3})(\]?)$/;	
		if( !regex.test(email) ) {
			$('.wpcf7-submit').hide();
		}
		else
		{
			$('.wpcf7-submit').show();
		}	
	});	

	// Validate email on contact form 7 (Register/en)
	$('#contact_form_email_20986').keyup(function(e) {
		var email = $('#contact_form_email_20986').val();
		// Validate only input number
		var regex = /^([\w-\.]+)@((\[[0-9]{1,3}\.[0-9]{1,3}\.[0-9]{1,3}\.)|(([\w-]+\.)+))([a-zA-Z]{2,4}|[0-9]{1,3})(\]?)$/;	
		if( !regex.test(email) ) {
			$('.wpcf7-submit').hide();
		}
		else
		{
			$('.wpcf7-submit').show();
		}	
	});

  $(window).keydown(function(event){
    if(event.keyCode == 13) {
      event.preventDefault();
      alert('The website not allow use enter key');
      return false;
    }
  });
			
});